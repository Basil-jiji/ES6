var isLoggedIn = true;
var isEmailVerified = true;
var cardInfo = true;

if (isLoggedIn && isEmailVerified && cardInfo) {
    console.log("You can purchase now");
}else{
    console.log("You havenot entered some details");
}